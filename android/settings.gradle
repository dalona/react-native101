pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven { url 'https://www.jitpack.io' }
    }
    includeBuild("../node_modules/@react-native/gradle-plugin")
}

rootProject.name = "FirstFirst"
include(":app")
